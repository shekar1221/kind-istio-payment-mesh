apiVersion: telemetry.istio.io/v1
kind: Telemetry
metadata:
  name: payments-telemetry
  namespace: payments
spec:
  accessLogging:
    - providers:
        - name: envoy
  tracing:
    - providers:
        - name: jaeger
      randomSamplingPercentage: 100
